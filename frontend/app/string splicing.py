#They're answer:
# def missing_char(str, n):
#     front = str[:n]
#     back = str[n+1:]
#
# return front + back

#my answer
def missing_char(str, n):
    str.splice[:n]
    str.splice[n+1:]

return missing_char('kitten',1)
return missing_char('kitten',0)
return missing_char('kitten',4)